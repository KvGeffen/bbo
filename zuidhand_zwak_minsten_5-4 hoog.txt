# zwak met minstens 5/4 hoog, geen regel van 20
(
  hcp(south) >= 6
  &&
  hcp(south) <= 10
  &&
  hcp(south) + spades(south) + hearts(south) < 20
  && 
  (
    (
      spades(south) >= 5
      &&
      hearts(south) >= 4
    )
    ||
    (
      spades(south) >= 4
      &&
      hearts(south) >= 5
    )
  )
)